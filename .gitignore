# Build da compilação web local (não versionar)
app/build/
app/.dart_tool/
app/.flutter-plugins-dependencies

# Venv de ferramentas (geração de ícone etc.) — não versionar
tools_venv/

# IDE / OS
.idea/
.DS_Store

# Segredos futuros (keystore de upload, quando formos à Play Store)
app/android/key.properties
app/android/app/*.jks
app/android/app/*.keystore
